# 專案工作偏好

## 核心目標

- 所有交付的實驗都必須讓其他人能依照文件重現。
- 優先採用簡單、直接、容易檢查的操作，不引入不必要的複雜流程。
- 不刪除或覆蓋既有程式與實驗資料；整併前先比較差異並保留來源。

## Server 分工

- `10.118.126.42`：主要整理與交付環境，也用於比較實驗。
- `10.118.126.40`：主要存放與執行預訓練相關內容。
- `10.118.126.41`：主要執行實驗。
- SSH：使用者 `tommy`、連接埠 `9453`，從 `.42` 使用既有金鑰連線。

## 驗證與交付方式

- 整理 `.40`、`.41` 的必要程式、設定、腳本與文件到 `.42`，再提交至 Git 遠端。
- 大型產物（例如 embedding、MalConv、FCGAT 相關資料或模型）不放 Git，另外整理並上傳 Google Drive。
- 每個實驗都要記錄環境、資料路徑、指令、設定、輸入與輸出位置。
- 耗時流程（例如 embedding 訓練或完整模型訓練）只做啟動與短程 smoke test，確認能執行後即停止，不等待完整訓練結束。
- 提交或搬移前先確認 Git 與各 server 的最新版本來源，避免舊資料蓋過新資料。
